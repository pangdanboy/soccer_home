// const { UserRoute } = require('./db/mongoose/models/userRoutes')
// const { routes, USER_PERMISSIONS } = require('./../global/config')
// 初始化数据
const initUserRoute = () => {
}

// 为不同角色用户分配路由
module.exports = {
  initUserRoute
}
